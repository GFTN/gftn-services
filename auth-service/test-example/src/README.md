# Example for running ts-node tests via mocha and chai

## Install deps 
```shell
# globals
npm install mocha chai ts-node --save-dev

# project
npm install mocha chai ts-node -g
npm i --save chai mocha ts-node tslint @types/mocha @types/chai
```